# PDF Upload Service

Простой веб-сервис на FastAPI для загрузки и просмотра PDF файлов.

## Возможности

- 📤 Загрузка PDF файлов через веб-интерфейс
- 👀 Прямой просмотр PDF файлов в браузере (как обычные PDF)
- 📱 Адаптивный дизайн для мобильных устройств

## Установка и запуск

1. Создайте виртуальное окружение:
```bash
python -m venv venv
```

2. Активируйте виртуальное окружение:
```bash
source venv/bin/activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Запустите сервер:

**Для разработки:**
```bash
python main.py
```

**Для продакшена (рекомендуется):**
```bash
gunicorn -c gunicorn.conf.py main:app
```

5. Откройте браузер и перейдите по адресу:
```
http://localhost:8000
```

## Использование

1. Перейдите на страницу `/upload-files` для загрузки PDF файлов
2. Нажмите "Выбрать файл" или перетащите PDF файл в область загрузки
3. После загрузки PDF файл откроется напрямую в браузере как обычный PDF документ
4. Используйте стандартные функции браузера для навигации, масштабирования и скачивания

## Структура проекта

```
├── main.py              # Основной файл приложения FastAPI
├── requirements.txt     # Зависимости Python
├── gunicorn.conf.py     # Конфигурация Gunicorn
├── templates/           # HTML шаблоны
│   ├── maintenance.html # Страница "временно недоступен"
│   └── index.html      # Страница загрузки файлов
└── uploads/            # Директория для загруженных файлов (создается автоматически)
```

## API Endpoints

- `GET /` - Главная страница (временно недоступен)
- `GET /upload-files` - Страница загрузки PDF файлов
- `POST /upload` - Загрузка PDF файла и перенаправление на просмотр
- `GET /pdf/{file_id}` - Получение PDF файла для прямого просмотра в браузере

## Технологии

- **FastAPI** - веб-фреймворк
- **Jinja2** - шаблонизатор
- **Uvicorn** - ASGI сервер
- **HTML5/CSS3/JavaScript** - фронтенд
